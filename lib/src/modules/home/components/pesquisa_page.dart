import 'package:flutter/material.dart';

class PesquisaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Conteúdo de Pesquisa'),
    );
  }
}
